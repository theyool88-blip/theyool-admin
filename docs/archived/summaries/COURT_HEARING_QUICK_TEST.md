# 법원 기일 관리 시스템 - 빠른 테스트 가이드

**Phase 1 완료 후 즉시 테스트 가능한 체크리스트**

---

## 🚀 빠른 시작

### 1. 개발 서버 실행
```bash
npm run dev
```

### 2. 로그인
- URL: http://localhost:3000/login
- 관리자 계정으로 로그인

---

## ✅ 기능별 테스트 시나리오

### **시나리오 1: 대시보드에서 법원 기일 추가**

1. **대시보드 접속**
   - URL: http://localhost:3000
   - "이번 주 일정 (D-7 이내)" 위젯 확인

2. **법원 기일 추가**
   - "+ 법원기일 추가" 버튼 클릭
   - **사건번호 검색**: 기존 사건번호 입력 (예: "2024드단12345")
   - **기일 유형**: "변론기일" 선택
   - **날짜**: 오늘부터 3일 후 날짜 입력
   - **시간**: 14:00 입력
   - **법정**: "서울가정법원 301호" 입력
   - **판사**: "김철수" 입력
   - "법원 기일 추가" 버튼 클릭

3. **결과 확인**
   - ✅ "법원 기일이 추가되었습니다" 알림
   - ✅ 대시보드 "이번 주 일정" 위젯에 새 기일 표시
   - ✅ D-3 배지 표시 (주황색)
   - ✅ ⚖️ 아이콘 표시 (빨강 배경)

---

### **시나리오 2: 사건 상세에서 데드라인 추가**

1. **사건 목록 접속**
   - URL: http://localhost:3000/cases
   - 사건번호가 있는 사건 클릭

2. **데드라인 탭 이동**
   - "데드라인" 탭 클릭
   - "+ 데드라인 추가" 버튼 클릭

3. **데드라인 추가**
   - **사건번호**: 자동으로 현재 사건번호가 입력되어 있음 (확인)
   - **데드라인 유형**: "항소기간 (14일)" 선택
   - **기산일**: 오늘 날짜 입력
   - **미리보기 확인**:
     - 기간: 14일
     - 만료일: 오늘부터 14일 후
     - 만료 일시: (만료일) 24:00
   - "데드라인 추가" 버튼 클릭

4. **결과 확인**
   - ✅ "데드라인이 추가되었습니다" 알림
   - ✅ 데드라인 목록에 새 항목 표시
   - ✅ D-14 배지 표시 (노랑)
   - ✅ "만료일" 정확히 계산되어 표시

---

### **시나리오 3: 월간 캘린더 통합 확인**

1. **월간 캘린더 접속**
   - URL: http://localhost:3000
   - 주간 캘린더 확인 (Dashboard 하단)

2. **확인 사항**
   - ✅ 법원 기일: 빨강 배경, ⚖️ 아이콘
   - ✅ 데드라인: 주황 배경, ⏰ 아이콘
   - ✅ 날짜별로 모든 일정이 정렬되어 표시
   - ✅ 클릭 시 상세 정보 확인 가능

---

### **시나리오 4: 상태 변경 및 삭제**

#### **법원 기일 완료 처리**
1. 사건 상세 → "법원기일" 탭
2. SCHEDULED 상태인 기일 찾기
3. "완료" 버튼 클릭
4. ✅ 상태가 "완료됨" (초록 배지)로 변경
5. ✅ "완료" 버튼 사라짐

#### **데드라인 완료 처리**
1. 사건 상세 → "데드라인" 탭
2. PENDING 상태인 데드라인 찾기
3. "완료" 버튼 클릭
4. ✅ 상태가 "완료됨" (초록 배지)로 변경
5. ✅ 완료일 표시
6. ✅ "완료" 버튼 사라짐

#### **삭제**
1. 임의의 법원 기일 또는 데드라인 선택
2. "삭제" 버튼 클릭
3. 확인 팝업에서 "확인" 클릭
4. ✅ "삭제되었습니다" 알림
5. ✅ 목록에서 즉시 제거

---

## 🐛 트러블슈팅

### **문제: 사건번호 자동완성이 작동하지 않음**
- **원인**: legal_cases 테이블에 court_case_number가 NULL
- **해결**: 사건 편집 페이지에서 사건번호 입력

### **문제: 데드라인 자동 계산이 안됨**
- **원인**: deadline_types 테이블에 데이터 없음
- **해결**:
  ```sql
  INSERT INTO deadline_types VALUES
    ('APPEAL_PERIOD', '항소기간', 14, '판결선고일로부터 14일'),
    ('FINAL_APPEAL_PERIOD', '상고기간', 14, '판결선고일로부터 14일'),
    ('BRIEF_SUBMISSION', '준비서면 제출기한', 7, '변론기일 7일 전'),
    ('EVIDENCE_SUBMISSION', '증거 제출기한', 7, '변론기일 7일 전');
  ```

### **문제: 대시보드에 일정이 표시되지 않음**
- **원인**: D-7 이내에 일정이 없음
- **해결**: 날짜를 오늘부터 3일 후로 설정하여 테스트

### **문제: 뷰(View) 데이터가 없음**
- **원인**: 뷰가 생성되지 않음
- **해결**: Supabase SQL Editor에서 뷰 생성 스크립트 실행
  ```sql
  -- upcoming_hearings 뷰 생성
  CREATE OR REPLACE VIEW upcoming_hearings AS
  SELECT
    id,
    case_number,
    hearing_type,
    hearing_date,
    location,
    status,
    (DATE(hearing_date) - CURRENT_DATE) AS days_until_hearing
  FROM court_hearings
  WHERE
    status = 'SCHEDULED'
    AND hearing_date >= NOW()
  ORDER BY hearing_date ASC;

  -- urgent_deadlines 뷰 생성
  CREATE OR REPLACE VIEW urgent_deadlines AS
  SELECT
    cd.id,
    cd.case_number,
    cd.deadline_type,
    dt.name AS deadline_type_name,
    cd.deadline_date,
    cd.deadline_datetime,
    cd.status,
    (cd.deadline_date - CURRENT_DATE) AS days_until_deadline
  FROM case_deadlines cd
  JOIN deadline_types dt ON cd.deadline_type = dt.type
  WHERE
    cd.status = 'PENDING'
    AND cd.deadline_date >= CURRENT_DATE
  ORDER BY cd.deadline_date ASC;
  ```

---

## 🔍 데이터베이스 검증

### **Supabase SQL Editor에서 직접 확인**

```sql
-- 1. 법원 기일 조회
SELECT * FROM court_hearings ORDER BY hearing_date DESC LIMIT 5;

-- 2. 데드라인 조회 (자동 계산 확인)
SELECT
  case_number,
  deadline_type,
  trigger_date,
  deadline_date,
  deadline_datetime,
  (deadline_date - trigger_date) AS calculated_days
FROM case_deadlines
ORDER BY created_at DESC
LIMIT 5;

-- 3. upcoming_hearings 뷰 확인 (D-7 이내)
SELECT * FROM upcoming_hearings WHERE days_until_hearing <= 7;

-- 4. urgent_deadlines 뷰 확인 (D-7 이내)
SELECT * FROM urgent_deadlines WHERE days_until_deadline <= 7;

-- 5. deadline_types 마스터 데이터 확인
SELECT * FROM deadline_types ORDER BY days DESC;
```

---

## 📊 예상 결과

### **대시보드 "이번 주 일정" 위젯**
```
📅 이번 주 일정 (D-7 이내)              [+ 법원기일 추가]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚖️ 변론기일                              [D-3]
   사건번호: 2024드단12345
   일시: 11.25 14:00 · 서울가정법원 301호

⏰ 항소기간                              [D-14]
   사건번호: 2024드단12345
   만료일: 2025-12-06
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### **사건 상세 - 법원기일 탭**
```
법원 기일                             [+ 기일 추가]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[변론기일] [예정됨]              25.11.25 14:00  [완료] [삭제]
📍 서울가정법원 301호
담당 판사: 김철수
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### **사건 상세 - 데드라인 탭**
```
데드라인                             [+ 데드라인 추가]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[항소기간] [대기중] [D-14]          만료일: 25.12.06  [완료] [삭제]
기산일: 2025-11-22
만료 일시: 2025-12-06 24:00
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## ✅ 최종 체크리스트

### **데이터베이스**
- [ ] `court_hearings` 테이블 존재
- [ ] `case_deadlines` 테이블 존재
- [ ] `deadline_types` 테이블 존재 및 데이터 삽입
- [ ] `upcoming_hearings` 뷰 생성
- [ ] `urgent_deadlines` 뷰 생성
- [ ] 트리거 `calculate_deadline_dates` 생성

### **기능 동작**
- [ ] 법원 기일 추가 (사건번호 자동완성)
- [ ] 데드라인 추가 (자동 계산 미리보기)
- [ ] 대시보드 D-7 이내 일정 표시
- [ ] 월간 캘린더 통합 표시
- [ ] 법원 기일 완료 처리
- [ ] 데드라인 완료 처리
- [ ] 법원 기일 삭제
- [ ] 데드라인 삭제

### **UI/UX**
- [ ] 모달 열기/닫기 정상 동작
- [ ] 폼 유효성 검사 동작
- [ ] 로딩 상태 표시
- [ ] 빈 상태 메시지 표시
- [ ] 에러 알림 표시
- [ ] 성공 알림 표시

---

## 🎯 다음 단계

Phase 1 테스트 완료 후:
1. **사용자 피드백 수집**: 실제 변호사/직원 사용성 테스트
2. **성능 최적화**: 대량 데이터 처리 테스트
3. **알림 시스템 구현**: 이메일/SMS 알림 (Phase 2)
4. **Toast 알림 도입**: alert() 대체

---

**마지막 업데이트**: 2025-11-22
**테스트 소요 시간**: 약 15분
**난이도**: ⭐⭐☆☆☆ (중하)
