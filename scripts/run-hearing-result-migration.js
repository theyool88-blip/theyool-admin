require('dotenv').config({ path: '.env.local' })
const fs = require('fs')
const path = require('path')

const migrationFile = path.join(__dirname, '../supabase/migrations/20251124_add_hearing_result.sql')
const sql = fs.readFileSync(migrationFile, 'utf8')

console.log('📋 변론기일 결과(result) 컬럼 추가 마이그레이션')
console.log('=' .repeat(60))
console.log()
console.log('✅ Supabase Dashboard → SQL Editor에서 아래 SQL을 실행하세요:')
console.log()
console.log('---')
console.log(sql)
console.log('---')
console.log()
console.log('💡 이 마이그레이션은:')
console.log('   1. hearing_result ENUM 타입 생성 (CONTINUED, CONCLUDED, POSTPONED, DISMISSED)')
console.log('   2. court_hearings 테이블에 result 컬럼 추가')
console.log('   3. 기존 데이터는 NULL로 유지 (나중에 모달에서 수정 가능)')
console.log()
